{
  "extends": "../tsconfig.json",
  "exclude": [""],
  "include": ["./*.test.ts"],
  "compilerOptions": {
    "noEmit": true,
    "paths": {
      // Make sure to also update aliases in the vitest config file
      "@src/*": ["src/*"]
    }
  }
}
